node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
.env.*
# Playwright test artefacts
test-results/

# Local working notes — never push to GitHub (migration plan, etc.)
RESUME.md
